# AtomISP raw formats are exposed by the kernel when they match the active
# sensor media-bus format; no module parameter is required.
options v4l2loopback devices=2 video_nr=10,11 card_label="Front Camera,Rear Camera" exclusive_caps=1,1 max_buffers=2
