# The raw gate remains off during driver probe. The DMI-gated preparation
# service enables it only after AtomISP and the Yoga Book sensors are present.
options v4l2loopback devices=2 video_nr=10,11 card_label="Front Camera,Rear Camera" exclusive_caps=1,1 max_buffers=2
